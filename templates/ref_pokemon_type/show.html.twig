{% extends 'base.html.twig' %}

{% block title %}RefPokemonType{% endblock %}

{% block body %}
    <h1>RefPokemonType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ ref_pokemon_type.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ ref_pokemon_type.name }}</td>
            </tr>
            <tr>
                <th>LastTrained</th>
                <td>{{ ref_pokemon_type.lastTrained ? ref_pokemon_type.lastTrained|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_ref_pokemon_type_index') }}">back to list</a>

    <a href="{{ path('app_ref_pokemon_type_edit', {'id': ref_pokemon_type.id}) }}">edit</a>

    {{ include('ref_pokemon_type/_delete_form.html.twig') }}
{% endblock %}
